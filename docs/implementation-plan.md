# 微信公众号自动化迁移实施计划

> 历史归档：本页记录早期单机迁移已经执行过的步骤，其中的旧绝对路径不再是当前项目要求。新用户请以根目录 `README.md` 和 `docs/migration-design.md` 为准。

## 已执行

- [x] 创建 `D:\wechat`。
- [x] 复制原 skill 到 `D:\wechat\skill`。
- [x] 复制原 skill 和 WorkBuddy 到 `D:\wechat\legacy` 作为回滚快照。
- [x] 创建内容、素材、运行记录和日志目录。
- [x] 创建统一配置、路径模块和检查入口。

## 后续承接顺序

1. 将历史文章生成逻辑从硬编码批处理脚本拆成参数化生成器。
2. 增加爆款候选获取、来源记录、去重和人工确认环节。
3. 将 CDP 公众号草稿上传逻辑接入 `app/`，从配置读取工作目录和端口。
4. 为正文 HTML、图片转存、草稿保存和封面设置增加小批量验证。
5. 完成从单篇到批量的可恢复运行和日志记录。
